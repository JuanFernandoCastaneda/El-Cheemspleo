import { useState } from "react"

/*
export const UpdatableInput: React.FC<{label: string, toUpdateProperty: string}> = ({label, toUpdateProperty}) => {
  const [inputValue, setInputValue] = useState()
}
*/
